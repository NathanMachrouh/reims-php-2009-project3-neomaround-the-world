{% extends 'base.html.twig' %}
{% block title %}University{{ university.id }}{% endblock %}

{% block body %}

<div class="media">
  <div class="media-body">
    <h1 class="mt-0">{{ university.name }}</h1>
  </div>
</div>
 
<a href="{{ path('university') }}">
    Retour à la liste des Pays
</a>

{% endblock %}